## Build Commands
- `cmake --preset debug && cmake --build --preset debug` to build under debug mode.
- `ctest --preset debug` to run test cases
- `cmake --build --preset debug -t format` to format codebase
- `cmake --preset release && cmake --build --preset release` to build under release mode.

## Code Style
- Use C++ 17 standard